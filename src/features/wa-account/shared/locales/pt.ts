import { WaAccountLocaleType } from './type'

export const ptWaAccountLocale: WaAccountLocaleType = {
  waAccountNotFound: 'Conta de whatsapp não encontrada.',
  waAccountWithSameAcronym: 'Já existe uma conta de whatsapp com a sigla .v..',
  waAccountWithSamePhoneNumber: 'Já existe uma conta de whatsapp com número .v..',
}
