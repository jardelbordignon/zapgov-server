import { WaAccountLocaleType } from './type'

export const enWaAccountLocale: WaAccountLocaleType = {
  waAccountNotFound: 'Whatsapp account not found.',
  waAccountWithSameAcronym: 'Already exists a whatsapp account with acronym .v..',
  waAccountWithSamePhoneNumber: 'Already exists a whatsapp account with number .v..',
}
